export interface Restaurant {
    id: number;
    name: string;
    moughataa: string;
    commune: string;
    latitude: number;
    longitude: number;
    utilisateur: any; // Replace with the appropriate type if needed
  }